<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KLVWWallpaper" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Transparent floating theme for tile config dialog activities -->
    <style name="Theme.KLVWWallpaper.TileConfig" parent="android:style/Theme.Translucent.NoTitleBar">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!-- Transparent overlay theme for the popup activity launched via startActivityAndCollapse. -->
    <style name="Theme.KLVWWallpaper.PopupOverlay" parent="Theme.KLVWWallpaper">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
